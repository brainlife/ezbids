
exports.setIntendedFor = $root=>{
    /*
    $root.subs.forEach(sub=>{
        sub.forEach(ses=>{
            ses.objects.forEach(object=>{
                if(object._type == "func/bold") {
                    //do stuff with $root.objects[0].metadata["SomeField"]
                     
                }
            });
        });
    })
    */
    console.log("TODO - go through objects and set intendedFor fields for the fmap objects");
}
